export default class userModel{

                constructor(id,name,email,password){

                        this.id=id;
                        this.nam=name;
                        this.email=email;
                        this.password=password;

                }

}